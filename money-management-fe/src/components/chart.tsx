import React, { ReactNode } from "react"
export interface ChartProps {
    type: string;
    data: [];
    title: string;
    sortBy: string;
    avgData: [];
}

export const Chart = ({ type, data, title, sortBy, avgData }) => {
    return (
        <></>
    );
}